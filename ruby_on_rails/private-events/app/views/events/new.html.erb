<h1>Create an Event</h1>
<br>
<%= render 'errors' if @event.errors.any? %>
<%= form_for @event do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_field :description %>

	<%= f.label :date %>
	<%= f.date_select :date %>

	<%= f.submit %>
<% end %>